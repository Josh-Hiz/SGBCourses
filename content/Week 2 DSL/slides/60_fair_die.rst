Fair Die Roll
=============

.. quizdown:: 

    # We want to simulate a 6-sided die using random. Choose the correct line to replace ``<missing code>``.

    ```python
    import random

    number = <missing code>

    print("You rolled the die and got the number", number)
    ```

    1. [x] ``random.randint(1, 6)``
    2. [ ] ``random.randint(1, 7)``
    3. [ ] ``random.randint(0, 6)``
    4. [ ] ``randint(1, 6)``